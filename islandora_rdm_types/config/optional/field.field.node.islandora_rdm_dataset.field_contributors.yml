langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_contributors
    - node.type.islandora_rdm_dataset
    - node.type.islandora_rdm_researcher
id: node.islandora_rdm_dataset.field_contributors
field_name: field_contributors
entity_type: node
bundle: islandora_rdm_dataset
label: Contributors
description: "The main researchers involved in producing the data, or the authors of the publication, in priority order. To supply multiple creators, repeat this property.\r\n\r\nThe institution or person responsible for collecting, managing, distributing, or otherwise contributing to the development of the resource.\r\nTo supply multiple contributors, repeat this property.\r\n\r\nFor software, if there is an alternate entity that \"holds, archives, publishes, prints, distributes, releases, issues, or produces\" the code, use the contributorType \"hostingInstitution\" for the code repository"
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:node'
  handler_settings:
    target_bundles:
      islandora_rdm_researcher: islandora_rdm_researcher
    sort:
      field: _none
    auto_create: false
    auto_create_bundle: ''
field_type: entity_reference

